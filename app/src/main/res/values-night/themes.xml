<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TaskWiz" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->


        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:statusBarColor">@color/create_task_root_view_background</item>
        <item name="android:navigationBarColor">@color/create_task_root_view_background</item>
    </style>

    <style name="DialogAnimation" >
        <item name="android:windowEnterAnimation">@anim/fragment_slide_up</item>
        <item name="android:windowExitAnimation">@anim/fragment_slide_down</item>
    </style>

</resources>